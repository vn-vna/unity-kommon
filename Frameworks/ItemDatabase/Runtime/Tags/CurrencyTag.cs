using UnityEngine;

namespace Com.Hapiga.Scheherazade.Common.ItemDatabase
{
    /// <summary>
    /// Example marker tag. No runtime data needed — presence on an
    /// ItemDefinition's metadata.tags list is sufficient.
    /// </summary>
    public class CurrencyTag : TagDefinition
    {
    }
}
